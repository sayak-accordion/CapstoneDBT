SELECT
    SUPPLIER_ID, 
    UPPER(SUPPLIER_NAME) AS supplier_name,
    UPPER(SUPPLIER_TYPE) AS supplier_type,
    TAX_ID,
    WEBSITE,
    UPPER(CREDIT_RATING) AS credit_rating,
    UPPER(PAYMENT_TERMS) AS payment_terms,
    UPPER(PREFERRED_CARRIER) AS preferred_carrier,
    IS_ACTIVE,
    YEAR_ESTABLISHED,
    LEAD_TIME_DAYS,
    MINIMUM_ORDER_QUANTITY,
    LAST_ORDER_DATE,
    LAST_MODIFIED_DATE,
    UPPER(CONTACT_PERSON) AS contact_person,
    CONTACT_EMAIL,
    CONTACT_PHONE,
    UPPER(CONTACT_ADDRESS) AS contact_address,
    CONTRACT_ID,
    CONTRACT_START_DATE,
    CONTRACT_END_DATE,
    CONTRACT_RENEWAL_OPTION,
    CONTRACT_EXCLUSIVITY,
    AVERAGE_DELAY_DAYS,
    DEFECT_RATE,
    ON_TIME_DELIVERY_RATE,
    RETURNS_PERCENTAGE,
    RESPONSE_TIME_HOURS,
    UPPER(QUALITY_RATING) AS quality_rating,
    CATEGORIES_SUPPLIED,
    -----------------------
    RECORD_HASH,
    EFFECTIVE_FROM,
    EFFECTIVE_TO,
    IS_CURRENT,
    LOAD_TIMESTAMP,
    FILENAME,
    FILE_ROW_NUMBER
FROM
    {{source('bronze','supplier_brnz')}}